#!/bin/bash
#PBS -P jk72
#PBS -q normal
#PBS -l walltime=03:00:00
#PBS -l mem=24GB
#PBS -l ncpus=2
#PBS -l wd
#PBS -l storage=gdata/fj7+scratch/jk72+gdata/jk72+gdata/yp75+gdata/gv90
#PBS -N {jobname}
#PBS -o {log_dir}/{jobname}.o
#PBS -e {log_dir}/{jobname}.e

source /g/data/jk72/jl0818/miniconda3_jo/etc/profile.d/conda.sh
conda activate NORMPROD

# added -u option for unbuffered processing, giving continuous logfile updates
python3 -u {run_script_path} "{img_pair_dir}" "{windows}" "{save_intermediate_products}" "{loglevel}"
