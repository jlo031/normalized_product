#!/bin/bash
#PBS -P jk72
#PBS -q normal
#PBS -l walltime=03:00:00
#PBS -l mem=24GB
#PBS -l ncpus=2
#PBS -l wd
#PBS -l storage=gdata/fj7+scratch/jk72+gdata/jk72+gdata/yp75+gdata/gv90
#PBS -N {jobname}
#PBS -o {log_dir}/{jobname}.o
#PBS -e {log_dir}/{jobname}.e

source {conda_sh}
conda activate {conda_env}

# added -u option for unbuffered processing, giving continuous logfile updates
python3 -u {run_script_path} "{img_pair_dir}" "{windows}" "{save_intermediate_products}" "{NP_min}" "{NP_max}" "{landmask_shapefile_path}" "{erode_landmask}" "{loglevel}"
